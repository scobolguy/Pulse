# Pulse
Reactive Event Language for ESPVM - mid-level IoT language with async events, HTTP server, MQTT pub/sub, and parallel execution as first-class constructs. ANTLR4 grammar. Compiles to ESPVM pcode.
